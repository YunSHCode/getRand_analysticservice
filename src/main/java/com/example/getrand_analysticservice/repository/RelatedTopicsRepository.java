package com.example.getrand_analysticservice.repository;

import com.example.getrand_analysticservice.dto.RelatedTopics;
import org.springframework.data.jpa.repository.JpaRepository;

public interface RelatedTopicsRepository extends JpaRepository<RelatedTopics, Long> {
}